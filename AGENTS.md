## Project instructions
- Keep all new logic modular and server-side unless explicitly asked for UI.
- Do not use the JavaScript && operator in inline logic.
- Prefer stable JSON response shapes for polling UIs.
- When data is not ready, return ok:false with a useful message instead of guessing.
- Do not break existing routes while adding new ones.
